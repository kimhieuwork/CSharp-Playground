<Window x:Class="WpfCrudApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WPF CRUD App" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,0">
            <Label Content="Product Name:"/>
            <TextBox x:Name="txtProductName" Margin="0,0,0,5"/>
            <Label Content="Price:"/>
            <TextBox x:Name="txtPrice" Margin="0,0,0,5"/>
            <Label Content="Quantity:"/>
            <TextBox x:Name="txtQuantity" Margin="0,0,0,5"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="btnAdd" Content="Add" Margin="5" Click="BtnAdd_Click"/>
                <Button x:Name="btnUpdate" Content="Update" Margin="5" Click="BtnUpdate_Click"/>
                <Button x:Name="btnDelete" Content="Delete" Margin="5" Click="BtnDelete_Click"/>
            </StackPanel>
        </StackPanel>

        <DataGrid x:Name="dgProducts" Grid.Row="0" Grid.Column="1" AutoGenerateColumns="True" IsReadOnly="True"/>
    </Grid>
</Window>